# MNIST-Digit-Classification-using-Neural-Network
MNIST Handwritten Digit Classification using a Neural Network built with TensorFlow/Keras. The model is trained on the MNIST dataset to recognize handwritten digits (0–9) through image preprocessing, training, and evaluation. Achieves high accuracy and demonstrates the fundamentals of deep learning and image classification.
